package two_sum

import (
	"fmt"
)

func main() {
	fmt.Println("LeetCode Go Project")

	// 示例：调用 two_sum 包中的函数
	// nums := []int{2, 7, 11, 15}
	// target := 9
	// result := two_sum.TwoSum(nums, target) // 注意：TwoSum 需要是导出的（首字母大写）
	// fmt.Printf("Two Sum for %v with target %d: %v\n", nums, target, result)
	// 为了能从 main 调用 solutions/0001_two_sum 中的 twoSum,
	// twoSum 函数名需要大写变为 TwoSum (导出函数)
	// 并且在 two_sum.go 中的 package 声明为 package two_sum
}
